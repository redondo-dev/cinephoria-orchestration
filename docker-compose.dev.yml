# cinephoria-orchestration/docker-compose.dev.yml
version: '3.9'

services:
  backend:
    build:
      context: ../cinephoria-backend  # Chemin relatif
      dockerfile: Dockerfile.dev
    volumes:
      - ../cinephoria-backend:/app
      - /app/node_modules
    # ...

  web:
    build:
      context: ../cinephoria-web
      dockerfile: Dockerfile.dev
    volumes:
      - ../cinephoria-web:/app
      - /app/node_modules
    # ...